"""Tech finder research (BL-1, research-log §4b): does the function-first,
within-archetype design recover known tech where the naive methods (§4c/§4d)
failed? Layer A (functional roles) is in ``archetypes.roles``; Layer B (the
within-archetype, matchup-conditioned role-effect estimator) is here."""
