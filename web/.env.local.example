# Base URL of the FastAPI read API (server-side fetch from Next). Use the
# composition-root entrypoint `serve:app` (not api.app) so /classify has the
# MTG classifier wired:
#   uvicorn serve:app --reload --port 8000
API_BASE=http://localhost:8000
